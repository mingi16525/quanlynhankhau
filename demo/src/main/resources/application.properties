# spring.application.name=demo
# # Cấu hình Datasource MySQL
# spring.datasource.url=jdbc:mysql://localhost:3306/QuanLyNhanKhauDB?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
# spring.datasource.username=root
# spring.datasource.password=admin
# # Đảm bảo mật khẩu này khớp với lệnh ALTER USER trong MySQL

# # Cấu hình Driver và JPA/Hibernate
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.hibernate.ddl-auto=update
# # Tắt auto-config Security
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
# # Dùng 'update' để Hibernate tự động tạo/cập nhật bảng (chỉ dùng cho Dev/Test)
# spring.jpa.show-sql=true
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# # JWT Configuration
# jwt.secret=your-very-secure-secret-key-here-1234567890
# jwt.expiration=86400000

# # Cấu hình Server (Nếu cần)
# server.port=8080